#include <bits/stdc++.h>
using namespace std;

void testcase() {
    int n, k; cin >> n >> k;
    int a[n];
    for(auto x : a) cin >> x;
    
}

int main() {
    int t; cin >> t;
    while(t--) testcase();
}